//this page is used for the time that the user searched for a path in application but it doesn't exist... so the user goes to that page

export default function NotFound() {   

return (   
<div>
    <h2>Page not NotFound</h2>
    <p>Could not find requested resourse</p>
</div>

)}